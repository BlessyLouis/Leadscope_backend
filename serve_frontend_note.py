"""
Optional: serve the frontend from Flask so you only deploy one app.
Copy index.html into a 'static' folder next to app.py and uncomment
the route below in app.py, OR just deploy the frontend on Netlify/Vercel
and point API_BASE in index.html to your Render backend URL.
"""
